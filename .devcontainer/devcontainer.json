{
  "name": "authenticator-with-laravel",
  "dockerComposeFile": "../build/docker-compose.yml",
  "remoteUser": "", // Set Current User (e.g. whoami -> 'username')
  "service": "server",
  "workspaceFolder": "/app"
}
